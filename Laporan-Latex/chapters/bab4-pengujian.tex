% ============================================================================
% BAB IV - PENGUJIAN DAN ANALISIS
% ============================================================================
\chapter{PENGUJIAN DAN ANALISIS}

Bab ini menyajikan hasil pengujian sistem yang difokuskan pada dua
skenario utama: (A) pengujian mekanisme \textit{read-write splitting}
untuk membuktikan bahwa query \textit{write} dan \textit{read}
diproses oleh \textit{server} yang berbeda, dan (B) pengujian
toleransi kesalahan (\textit{fault tolerance}) dengan mematikan
salah satu Replica secara sengaja untuk memastikan aplikasi tetap
berjalan. Setiap skenario dilengkapi dengan kronologi pengujian, bukti
log, dan analisis hasil. Kedua skenario ini dipilih karena mewakili
dua aspek paling kritis dari arsitektur \textit{read-write splitting}:
kebenaran fungsional (apakah query diarahkan ke node yang tepat) dan
ketahanan sistem (apakah sistem tetap berfungsi saat komponen gagal).

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/all docker up.png}
\caption{Status Kontainer Docker Arsitektur Komposin yang Sedang Berjalan Aktif}
\label{fig:all-docker-up}
\end{figure}

Status kontainer pada Gambar~\ref{fig:all-docker-up} menunjukkan bahwa
seluruh 5 layanan utama (\texttt{pg-master}, \texttt{pg-replica1}, 
\texttt{pg-replica2}, \texttt{pgpool}, dan \texttt{bdt-app}) telah
berjalan secara sehat (\textit{healthy}) pada lingkungan Docker.

\section{Pengujian Read-Write Splitting (Skenario A)}

Pengujian ini bertujuan membuktikan bahwa Pgpool-II berhasil
memisahkan query berdasarkan jenis operasinya. Secara spesifik, query
\textit{write} (\texttt{INSERT}, \texttt{UPDATE}, \texttt{DELETE})
harus diproses oleh \textbf{PostgreSQL Master} (172.25.0.10),
semua query \textit{read} (\texttt{SELECT}) harus diproses secara
\textit{load-balanced} oleh \textbf{PostgreSQL Replica} (172.25.0.11 atau 
172.25.0.12). Bukti pemisahan ini diperoleh dari log Pgpool-II dengan 
\texttt{log\_per\_node\_statement = on} (menunjukkan node mana yang 
memproses setiap statement) dan status statistik pada Pgpool-II.

\subsection{Pengujian Query Write}

\subsubsection{Operasi INSERT}

Pengujian dilakukan dengan menjalankan query \texttt{INSERT} melalui
aplikasi (via Pgpool-II) untuk menambahkan data produk baru pada
tabel \texttt{produk}. Aplikasi Laravel akan menghasilkan query ini
saat Admin mengisi formulir penambahan produk pada halaman manajemen
katalog. Pgpool-II mengenali \texttt{INSERT} sebagai operasi \textit{write}
dan meneruskannya ke backend 0 (Master).

\textbf{Query yang dijalankan:}
\begin{lstlisting}[language=SQL, caption={Query INSERT: Menambah Produk Baru}]
INSERT INTO produk (nama, harga, stok, deskripsi, gambar,
                    created_at, updated_at)
VALUES ('Pupuk Kompos Organik', 25000, 100,
        'Pupuk organik hasil pengolahan sampah',
        'pupuk_a.jpg', NOW(), NOW());
\end{lstlisting}

\textbf{Hasil Pengujian:} Data berhasil ditambahkan ke database
dan aplikasi menampilkan produk baru pada halaman katalog. Tidak ada
error dari sisi aplikasi yang menunjukkan bahwa operasi write berhasil
dieksekusi.

\textbf{Bukti Log Master (Aktual):}

Sebagai bukti aktual bahwa operasi penulisan data diarahkan langsung ke PostgreSQL Master, Gambar~\ref{fig:log-insert} menunjukkan screenshot dari log Pgpool-II yang mencatat query \texttt{INSERT} yang diproses pada Node ID 0 (\texttt{pg-master}).

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/insert .png}
\caption{Log Pgpool-II Memproses Query INSERT pada Node 0 (pg-master)}
\label{fig:log-insert}
\end{figure}

Log di atas membuktikan bahwa query \texttt{INSERT} diproses oleh
PostgreSQL Master. Hal ini terlihat dari indikator \texttt{DB node id: 0}
yang tertera pada log Pgpool-II. Tidak ada entri log serupa yang muncul
di log Replica 1 maupun Replica 2, membuktikan bahwa Replica tidak menerima
query write.

\subsubsection{Operasi UPDATE}

Pengujian dilakukan dengan menjalankan query \texttt{UPDATE} untuk
mengubah data produk, misalnya saat Admin memperbarui harga atau stok
produk melalui halaman manajemen katalog. Seperti halnya
\texttt{INSERT}, query \texttt{UPDATE} harus diproses oleh Master
karena merupakan operasi yang mengubah data.

\textbf{Query yang dijalankan:}
\begin{lstlisting}[language=SQL, caption={Query UPDATE: Mengubah Data Produk}]
UPDATE produk SET harga = 30000, stok = 150 WHERE id = 1;
\end{lstlisting}

\textbf{Bukti Log Master:}
\begin{lstlisting}[caption={Log PostgreSQL Master: Query UPDATE}, label={lst:log-update}]
2026-06-21 06:28:05.785: pgAdmin 4 - CONN:5332347 pid 66: LOG:  DB node id: 0 backend pid: 494 statement: Execute: UPDATE public.produks SET ...
\end{lstlisting}

Log Master kembali menunjukkan bahwa query \texttt{UPDATE} diproses
oleh Master (Node 0).

\subsubsection{Operasi DELETE}

Pengujian dilakukan dengan menjalankan query \texttt{DELETE} untuk
menghapus data produk. Operasi ini harus diproses oleh Master
karena merupakan operasi yang mengubah data.

\textbf{Query yang dijalankan:}
\begin{lstlisting}[language=SQL, caption={Query DELETE: Menghapus Data Produk}]
DELETE FROM produk WHERE id = 1;
\end{lstlisting}

\textbf{Bukti Log Master:}
\begin{lstlisting}[caption={Log PostgreSQL Master: Query DELETE}, label={lst:log-delete}]
2026-06-21 06:28:10.115: pgAdmin 4 - CONN:5332347 pid 66: LOG:  DB node id: 0 backend pid: 494 statement: Execute: DELETE FROM public.produks WHERE id = 1;
\end{lstlisting}

\subsubsection{Analisis Hasil Pengujian Query Write}

Berdasarkan ketiga pengujian di atas, seluruh query \textit{write}
(\texttt{INSERT}, \texttt{UPDATE}, \texttt{DELETE}) tercatat pada log
PostgreSQL Master (Node 0). Tidak ada satupun query write yang didelegasikan
ke Replica. Hal ini membuktikan bahwa Pgpool-II berhasil mengarahkan
query \textit{write} ke Master (backend 0) secara transparan sesuai
konfigurasi \texttt{master\_slave\_mode = on}.

\subsection{Pengujian Query Read}

\subsubsection{Operasi SELECT}

Pengujian dilakukan dengan menjalankan query \texttt{SELECT}
melalui aplikasi untuk mensimulasikan aktivitas pengguna yang melihat
katalog produk, membaca artikel, dan mengecek riwayat transaksi.
Pgpool-II harus mengarahkan seluruh query ini ke Replica (backend 1 atau 2),
bukan ke Master (backend 0).

\textbf{Query yang dijalankan:}
\begin{lstlisting}[language=SQL, caption={Query SELECT: Membaca Data Katalog dan Artikel}]
SELECT * FROM public.artikels;
\end{lstlisting}

\textbf{Bukti Log Replica (Aktual):}

Sebagai bukti aktual bahwa operasi pembacaan data diarahkan ke Replica dan didistribusikan secara load-balanced, Gambar~\ref{fig:select-distribution} menunjukkan query \texttt{SELECT} yang diproses oleh \texttt{pg-replica1} (Node 1) dan \texttt{pg-replica2} (Node 2).

\begin{figure}[H]
\centering
\begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/select pgreplica1.png}
    \caption{Query SELECT Terbaca pada pg-replica1 (Node 1)}
    \label{fig:select-replica1}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/select pgreplica2.png}
    \caption{Query SELECT Terbaca pada pg-replica2 (Node 2)}
    \label{fig:select-replica2}
\end{subfigure}
\caption{Log Distribusi Query SELECT oleh Pgpool-II ke Server Replica}
\label{fig:select-distribution}
\end{figure}

Log di atas menunjukkan bahwa query \texttt{SELECT} diproses oleh
PostgreSQL Replica. Indikator \texttt{DB node id: 1} menunjukkan query
diarahkan ke Replica 1, sedangkan \texttt{DB node id: 2} menunjukkan query
diarahkan ke Replica 2. Hal ini mengonfirmasi bahwa \textit{load balancing}
berfungsi sesuai bobot yang dikonfigurasi (\texttt{backend\_weight0 = 0} 
untuk Master, dan \texttt{backend\_weight1 = 1}, \texttt{backend\_weight2 = 1} 
untuk masing-masing Replica).

\subsubsection{Bukti Monitoring Koneksi (SHOW pool\_nodes)}

Perintah \texttt{SHOW pool\_nodes} yang dijalankan pada Pgpool-II
menampilkan status \textit{real-time} dari seluruh \textit{backend}.
Output ini memberikan gambaran ringkas tentang distribusi beban dan
status kesehatan setiap node.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/all node up.png}
\caption{Status Semua Node Database Aktif Berjalan pada Pgpool-II}
\label{fig:all-node-up}
\end{figure}

Output \texttt{SHOW pool\_nodes} pada Gambar~\ref{fig:all-node-up} menunjukkan bahwa
\texttt{node\_id 0} (Master) memiliki \texttt{lb\_weight = 0.000000} dan 
tidak melayani pembacaan (\texttt{load\_balance\_node = false}). 
Sementara itu, \texttt{node\_id 1} dan \texttt{node\_id 2} (Replica) 
memiliki status \texttt{up} dan \texttt{lb\_weight = 0.500000}, membuktikan 
bahwa seluruh query \texttt{SELECT} dilayani oleh Replica secara seimbang.

\subsection{Kesimpulan Pengujian Skenario A}

Pengujian \textit{read-write splitting} membuktikan bahwa mekanisme
berjalan sesuai rancangan. Seluruh query \textit{write}
diproses oleh PostgreSQL Master (node 0). Seluruh query \textit{read}
(\texttt{SELECT}) diproses oleh PostgreSQL Replica (node 1 dan node 2) 
dengan distribusi yang seimbang. Keberhasilan ini dicapai secara transparan 
tanpa modifikasi kode aplikasi sedikitpun.

\section{Pengujian Toleransi Kesalahan / Replica Down (Skenario B)}

Pengujian ini bertujuan membuktikan bahwa sistem tetap dapat
beroperasi meskipun salah satu PostgreSQL Replica mengalami kegagalan
(\textit{down}). Skenario ini menguji kemampuan Pgpool-II dalam
mendeteksi kegagalan (\textit{health check}) dan mengalihkan traffic 
(\textit{failover}) secara otomatis ke node yang tersisa tanpa 
menyebabkan kegagalan total pada sistem aplikasi.

\subsection{Kronologi Pengujian}

\begin{enumerate}
    \item \textbf{Kondisi Awal:} Seluruh \textit{backend} (Master,
    Replica 1, Replica 2) dalam keadaan \texttt{up} dan berfungsi
    normal (Gambar~\ref{fig:all-node-up}). Aplikasi berjalan normal.
    \item \textbf{Mematikan salah satu Replica:} Menghentikan kontainer
    Replica 1 dengan perintah \texttt{docker compose stop pg-replica1}
    atau Replica 2 dengan \texttt{docker compose stop pg-replica2}.
    \item \textbf{Deteksi Kegagalan \& Failover:} Pgpool-II mendeteksi 
    kegagalan melalui \textit{health check} periodik, lalu melepaskan 
    (\textit{detach}) node tersebut dari pool dan mengubah statusnya 
    menjadi \texttt{down}.
    \item \textbf{Pengalihan Traffic:} Pgpool-II mengalihkan seluruh query 
    \texttt{SELECT} yang seharusnya diproses oleh replica yang mati ke 
    replica yang masih aktif.
    \item \textbf{Verifikasi Aplikasi:} Aplikasi tetap dapat melayani 
    permintaan pembacaan data katalog dan transaksi tanpa mengalami gangguan.
\end{enumerate}

\subsection{Kondisi Sistem Sebelum Replica Dimatikan}

Sebelum salah satu replica dimatikan, semua node berada dalam keadaan aktif (\texttt{up}). Gambar~\ref{fig:status-before-down} menyajikan bukti status normal sistem.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/all node up.png}
\caption{Status Semua Node Database Aktif Sebelum Uji Toleransi Kesalahan}
\label{fig:status-before-down}
\end{figure}

Semua \textit{backend} dalam status \texttt{up} dan berfungsi normal. Beban
pembacaan dialokasikan secara bergantian ke Replica 1 dan Replica 2.

\subsection{Kondisi Sistem Setelah Replica Dimatikan}

Setelah salah satu kontainer replica dimatikan secara sengaja, Pgpool-II mendeteksi kegagalan tersebut melalui pemeriksaan kesehatan periodik. Status node yang bersangkutan akan berubah menjadi \texttt{down}. 

Gambar~\ref{fig:status-replica-down} menyajikan bukti status sistem saat masing-masing replica dimatikan.

\begin{figure}[H]
\centering
\begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pgreplica1 down.png}
    \caption{Status Node 1 (pg-replica1) Down}
    \label{fig:status-replica1-down}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/pgreplica2 down.png}
    \caption{Status Node 2 (pg-replica2) Down}
    \label{fig:status-replica2-down}
\end{subfigure}
\caption{Status Node Database Setelah Salah Satu Replica Dimatikan}
\label{fig:status-replica-down}
\end{figure}

\textbf{Analisis Failover:} 
* Pada Gambar~\ref{fig:status-replica1-down}, ketika Node 1 (\texttt{pg-replica1}) mati, statusnya terbaca sebagai \texttt{down}. Namun, Node 0 dan Node 2 tetap aktif (\texttt{up}), dan Pgpool-II mengalihkan query read ke Node 2 (\texttt{load\_balance\_node = true}).
* Pada Gambar~\ref{fig:status-replica2-down}, ketika Node 2 (\texttt{pg-replica2}) mati, statusnya terbaca sebagai \texttt{down}. Pgpool-II langsung memindahkan peran penyeimbang beban ke Node 1 (\texttt{load\_balance\_node = true}).
* Jika kedua replica mati sekaligus, Pgpool-II secara otomatis melakukan \textit{graceful degradation} dengan mengalihkan seluruh query read-write ke Node 0 (Master) agar aplikasi tetap berjalan tanpa \textit{downtime}.

\subsection{Bukti Aplikasi Tetap Berjalan}

Setelah salah satu replica dimatikan, pengujian fungsional pada aplikasi
menunjukkan seluruh fitur tetap berfungsi normal. Halaman Katalog
Produk tetap dapat diakses dan menampilkan daftar produk karena query
\texttt{SELECT} kini dilayani oleh replica yang aktif.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{images/dashboard aplikasi.png}
\caption{Tampilan Dashboard Aplikasi Tetap Dapat Diakses Tanpa Gangguan}
\label{fig:dashboard-app}
\end{figure}

Gambar~\ref{fig:dashboard-app} membuktikan bahwa dashboard aplikasi Komposin
tetap dapat melayani permintaan web secara penuh meskipun salah satu
layanan replica database sedang dihentikan secara sengaja.

\subsection{Kesimpulan Pengujian Skenario B}

Pengujian toleransi kesalahan membuktikan bahwa sistem memiliki
ketahanan terhadap kegagalan komponen. Pgpool-II berhasil mendeteksi
kegagalan Replica melalui mekanisme \textit{health check} periodik.
Pgpool-II secara otomatis mengalihkan \textit{traffic} query \texttt{SELECT} 
ke Replica yang masih aktif (atau ke Master jika semua replica mati) tanpa 
menyebabkan kegagalan pada aplikasi, sehingga menjamin ketersediaan layanan 
(\textit{high availability}) pada aplikasi Komposin.
